import { FC } from "react";
import { classNames } from "../../utils";

const MainPage: FC<{ className?: string }> = ({ className, children }) => {
  return <div>123{children}</div>;
};

export default MainPage;
